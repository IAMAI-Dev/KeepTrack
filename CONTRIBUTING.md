# 贡献指南

感谢你愿意为 KeepTrack 贡献代码、文档或数据。本指南用于统一
项目协作方式，让每一次修改都更容易理解、审查和维护。

KeepTrack 目前是一个基于 Python 和 Tkinter 的桌面工具，核心代码
位于 `main.py`，依赖项记录在 `pyproject.toml` 中。

## 开始之前

1. Fork 或克隆仓库到本地。
2. 建议使用 Python 3.8 或更高版本。
3. 安装依赖：

   ```bash
   uv sync
   ```

4. 运行项目：

   ```bash
   uv run main.py
   ```

提交前请至少确认程序可以正常启动，并尽量覆盖你修改过的功能路径。

## 贡献范围

欢迎以下类型的贡献：

- 修复程序运行错误、界面异常或数据生成问题。
- 改进跑步轨迹、配速、心率、步频等模拟逻辑。
- 补充或修正 `Location_data.txt` 中的场地数据。
- 改进 README、贡献指南、注释和其他文档。
- 优化代码结构、可读性和可维护性。

请避免在同一个提交中混合多个无关改动。例如，不要把 Bug 修复、
界面调整和大段格式化放在同一次提交里。

## Commit 规范

`type` 请使用英文，提交信息请使用中文，并遵循以下格式：

```text
<type>: <描述>

[详细描述改了什么，为什么要改]

Signed-off-by: Name <email>
```

标题要求：

- 使用祈使语气，描述这次提交要做什么。
- 不超过 50 个字。
- `<type>` 后使用英文冒号和一个空格。
- 标题末尾不加句号。

正文要求：

- 说明改了什么，以及为什么要改。
- 每一行不超过 50 个字。
- 如果改动很小，可以省略正文。
- 正文和 `Signed-off-by` 之间保留一个空行。

签名要求：

- 每个提交都应包含 `Signed-off-by`。
- 可以使用以下命令自动追加签名：

  ```bash
  git commit -s
  ```

建议使用以下前缀：

- `feat`: 新功能
- `fix`: 修复 Bug
- `docs`: 文档更新
- `style`: 代码格式修改，不影响逻辑
- `refactor`: 重构代码
- `chore`: 杂务，例如修改 gitignore 或依赖管理

示例：

```text
fix: 修复批量生成时的时间间隔计算

批量生成多个 FIT 文件时，后续文件的开始时间
没有正确应用用户设置的时间间隔。

Signed-off-by: 张三 <zhangsan@example.com>
```

另一个示例：

```text
docs: 完善贡献指南中的注释规范

补充功能性注释、解释性注释和标记性注释
的写法，方便贡献者保持一致的代码风格。

Signed-off-by: 李四 <lisi@example.com>
```

## 代码风格

本项目的 Python 代码风格尽量遵循 PEP 8，并结合项目现状采用以下
约定。

### 行长度

- 代码每行不超过 79 个字符。
- 文档字符串和注释每行不超过 72 个字符。
- 过长的表达式应使用括号换行，而不是使用反斜杠续行。

推荐：

```python
self.file_output = tk.StringVar(
    value=os.path.join(
        os.path.expanduser("~"),
        "Desktop",
        "Keep运动数据",
    )
)
```

### 缩进

- 使用 4 个空格缩进。
- 禁止使用 Tab。
- 空行应保持简洁，不要为了视觉分隔添加过多空行。

### 续行

续行应对齐到上一行的参数起始位置，或使用悬挂缩进。

推荐：

```python
ttk.Entry(
    basic_frame,
    textvariable=self.run_distance,
).grid(row=0, column=1, sticky="ew", padx=15)
```

也可以在参数较少时对齐到左括号之后：

```python
messagebox.showerror("输入错误",
                     "请输入有效的距离和时长。")
```

在新代码中优先使用第一种写法，通常更易读，也更方便后续扩展。

### 导入顺序

导入应放在文件顶部，并按以下顺序分组：

1. 标准库导入。
2. 相关第三方库导入。
3. 本地应用或库特定导入。

每组之间保留一个空行。避免在函数内部导入，除非这样做可以解决
循环依赖、明显降低启动成本，或有其他清晰理由。

示例：

```python
import math
import os
from datetime import datetime

from fit_tool.fit_file_builder import FitFileBuilder

from keeptrack.paths import get_default_output_dir
```

### 命名规范

- 变量和函数使用 `snake_case`。
- 类名使用 `PascalCase`。
- 常量使用 `UPPER_CASE_WITH_UNDERSCORES`。
- 私有辅助方法使用单下划线前缀，例如 `_build_ui`。
- 命名应表达含义，避免使用 `a`、`b`、`tmp` 等模糊名称。

如果现有代码中存在拼写错误或历史命名，修改时应谨慎处理。除非本次
变更确实需要，否则不要为了命名统一而引入大范围重命名。

### 注释规范

注释请保持清晰、准确，并与实际逻辑同步。不要保留失效、乱码或
与代码行为不一致的注释。新增注释时，请根据用途分为以下三类。

#### 功能性注释

功能性注释用于标识一个功能区域、模块职责或一组相关方法。它通常
解释“这里负责什么”，可以使用三引号，并允许多行书写。

示例：

```python
""" UI 辅助方法 """

def _ui(self, fn, *args, **kwargs):
    self.root.after(0, lambda: fn(*args, **kwargs))
```

功能性注释应简洁，不要写成过长的说明文。如果某段功能已经可以通过
类名、函数名或变量名清楚表达，可以不额外添加。

#### 解释性注释

解释性注释用于说明某一行或某一小段代码的意图，重点解释“为什么
这样做”，而不是重复“代码做了什么”。解释性注释使用 `#`，一般
写成单行。

示例：

```python
# 在主线程中执行 UI 更新，避免 Tkinter 线程安全问题
def _ui(self, fn, *args, **kwargs):
    self.root.after(0, lambda: fn(*args, **kwargs))
```

如果解释内容超过一行，请拆成多行 `#` 注释，并保证每行不超过
72 个字符。

#### 标记性注释

标记性注释用于记录待办、风险、临时方案或需要复查的地方。它应以
固定关键词开头，格式为 `# KEYWORD: 说明`。

建议使用以下关键词：

- `TODO`: 待办事项，表示功能尚未完成或计划未来添加。
- `FIXME`: 已知问题，表示当前实现需要修复。
- `NOTE`: 重要说明，提醒维护者注意上下文或限制。
- `HACK`: 临时方案，后续应替换为更稳妥的实现。

示例：

```python
# TODO: 支持从配置文件读取默认操场坐标
# FIXME: 批量生成失败时需要回滚已创建的部分文件
# NOTE: FIT 时间戳需要使用本地时间换算后的结果
```

标记性注释应写清楚背景或下一步动作。解决对应问题后，请同步删除
或更新该注释。

### 函数和类

- 函数应尽量短小，职责单一。
- 复杂逻辑应拆分为具名辅助函数。
- UI 构建、输入校验、数据计算和 FIT 文件生成逻辑应尽量分层。
- 避免在一个函数中同时处理界面展示、业务计算和文件写入。

### 异常处理

- 捕获异常时应尽量捕获具体异常类型。
- 不要静默吞掉异常。
- 面向用户的错误应通过界面提示或日志给出可理解的信息。
- 面向开发者的错误应保留足够上下文，方便定位问题。

### 文件和资源

- 不要提交本地生成的 FIT 文件、临时文件、缓存目录或虚拟环境。
- `assets/` 目录用于项目展示和文档资源，不要放入无关截图。
- 新增依赖时，请同步更新 `pyproject.toml`，并说明引入原因。

### 操场位置信息贡献

`Location_data.txt` 记录项目已收录的操场轨迹基础信息。当前文件只
提供了一部分操场的位置数据，其余操场需要贡献者自行查阅。

如果你愿意补充新的操场信息，欢迎将准确数据提交到
`Location_data.txt`。建议使用谷歌地球等地图工具确认以下信息：

- 学校名称和校区名称。
- 操场名称或可辨识的位置描述。
- 操场中心点的纬度和经度。
- 跑道长轴相对正北方向的方位角。

提交前请尽量确认数据准确，避免只凭截图或粗略目测填写。新增数据
应保持与文件中现有格式一致，方便后续维护和查阅。

如果不确定方位角，可以在 PR 中说明测量方式和不确定点，方便维护者
复核。不要提交来源不明、明显偏移或无法定位到具体操场的数据。

## 测试和检查

当前项目尚未建立自动化测试流程。提交前请至少完成以下检查：

1. 运行 `python main.py`，确认程序可以启动。
2. 检查你修改过的输入项、按钮和生成流程。
3. 如果修改了 FIT 生成逻辑，请生成一个测试文件并确认无异常。
4. 如果修改了文档，请检查 Markdown 渲染是否正常。
5. 如果新增依赖，请在干净环境中验证 `uv sync`。

如果未来添加测试，请优先使用 `pytest`，并将测试放在 `tests/`
目录中。测试文件命名建议使用 `test_*.py`。

## 开发流程

建议按照以下流程提交贡献：

1. Fork 本仓库，并克隆到本地。
2. 基于最新代码创建特性分支：

   ```bash
   git checkout -b feature/AmazingFeature
   ```

3. 完成修改，并进行必要的本地检查。
4. Commit 你的修改，确保提交信息清晰且符合规范。
5. Push 到你的远程分支：

   ```bash
   git push origin feature/AmazingFeature
   ```

6. Open a Pull Request。

分支名称建议表达改动目的，例如 `feature/location-data`、
`fix/batch-time-interval` 或 `docs/contributing-guide`。

## Pull Request 建议

提交 Pull Request 时，请尽量包含：

- 本次修改的目的。
- 主要改动点。
- 已完成的测试或手动检查。
- 可能影响用户的行为变化。
- 关联的 Issue，如果有的话。

PR 应保持聚焦。较大的改动建议先拆分为多个较小的 PR，或者先开
Issue 讨论设计方向。

## 安全和使用边界

本项目仅供学习和交流使用。请不要提交用于商业滥用、破坏平台公平性
或规避平台规则的功能。涉及数据生成逻辑的改动，应尽量保持透明、
可审查，并说明其用途。
